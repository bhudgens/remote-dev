# Shared configuration for remote-dev scripts
# Source this file from other scripts: source "$(dirname "$0")/../config.env"

# Chrome DevTools Protocol
CDP_PORT=9222

# Chrome executable path (from WSL2)
CHROME_PATH="/mnt/c/Program Files/Google/Chrome/Application/chrome.exe"

# Separate Chrome profile for automation (avoids interfering with normal browsing)
CHROME_USER_DATA_DIR="Chrome-Automation"

# NetBird executable (Windows, accessed from WSL2)
NETBIRD_EXE="/mnt/c/Program Files/NetBird/netbird.exe"

# NetBird peer name for the remote EC2 instance
EC2_PEER_NAME="cloud-development"

# Default SSH user on the EC2 instance
EC2_SSH_USER="ubuntu"

# VS Code code relay port (reverse tunnel for remote `code .` command)
CODE_RELAY_PORT=9223
